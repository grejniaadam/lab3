# Changelog

## 2026-06-21 — Initial scaffold and docs
- Added `mailer` package with email sending, subscribers, templates and web UI.
- Added basic templates under `templates/email`.
- Added test suite (`tests/`) and Test Runner UI (`/tests`).
- Added documentation under `docs/`.
